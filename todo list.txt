1) создаю переменную form которая хранит form
2) создаю пустой массив todos
3) создаю функцию creatTask 
4) внутри функции создаю перемнную message
5) внутри функции создаю объект task
6) внутри объекта создаю ключ id передаю значение длины массива+1 (id: todos.length+1), 
		  создаю ключ message передаю значение переменной message(message: message.value).

7) в массив todos пушим объект task (todos.push(task))
8) вызываю функцию renderTodos() которая будет создана позже
9) на form вешаю событие 'submit', создаю анонимную функию прописываю в скобках функии 'e'
10) в теле функии обнуляю настройки формы (e.preventDefault)
11) вызываю функию creatTask которая пушит в массив (todos) данные введеные пользователем 
12) создаем функию renderTodos которая создает нам параграх и пушит ее в div что бы отобразить на странице
13) создаем переменную output которая хранит в себе нужный нам див
14) очищаем содержимое нашего дива что бы при создании нового параграфа не отрисовывался прошлый
15) пропускаем массив через цикл forEach
16) в теле фикла создаем переменную mess которая создает и хранит в себе html элемент 'p'
17) для этого элемента присваем значение нашего объекта (task) которая уже хранится в масииве (todos): `${el.id} ${el.message}`
18) результат аппендим в переменную output которая является нашей дивкой


filter

1) вешаем событие клик на кнопку удаления
2) вызываем внутри функцию филтр с аргументом el.id
3) создаем функцию deleteTodo с аргументом id 
4) в теле функции фильтруем todos и переписываем сам массивы todos (todos = todos.filter(el=>el.id != id)
5) в фильтре вызываем стрелочную функию в условиях филтра сравниваем id элемента с id переданным в аргументе el=>el.id != id
6) вызываем функцию renderTodos которая перерисует нам todos


id: todos.length === 0? 1
    :todos[todos.length-1].id+1,

1) если todos.length равен 0 то id будет равен 1
2) иначе мы берем массив todos в его индексе у длины массива отнимаем единичку после берем id а этот айди будет являться айди 
   предыдудущего элемента это и прибовляем единичку и присваем это значение для id текущего элемента





















